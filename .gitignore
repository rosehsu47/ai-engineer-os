# OS cruft
.DS_Store

# panel/ (Go) build artifacts
panel/aios-panel
panel/*.exe
panel/*.test

# shell/editor scratch
*.log
*.tmp
*~

# this repo's own .ai/ is runtime cruft (it is not an installed workspace;
# the real templates live in templates/ai/)
/.ai/
